# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# env — ห้าม commit API key / secret
.env
.env.local
.env*.local

# logs
*.log
npm-debug.log*

# misc
.DS_Store
*.pem
.vscode/
.idea/

# Claude Code — เครื่องมือ dev ฝั่ง local ไม่เกี่ยวกับ runtime ของเว็บ
.claude/

# drizzle journal meta ok to commit, but keep local snapshots out if desired
