-- เก็บ "เลขที่ในห้อง" ของสมาชิกไว้ตอนสมัคร เพื่อพิมพ์ลงเอกสาร (ใบรายชื่อ/ใบกรอกคะแนน/ใบประกาศผล)
--
-- ทำไมต้องเก็บ ไม่ถาม SchoolOS สด ๆ ตอนพิมพ์:
-- enrollments ของ SchoolOS แยกตามปีการศึกษา — พอขึ้นปีใหม่ เด็กที่จบ/ลาออกไปแล้วจะไม่มีแถวของปีใหม่
-- ถามกลับไปก็ไม่เหลืออะไรให้ตอบ เอกสารย้อนหลังจะพิมพ์เลขที่ไม่ได้อีกเลย
-- (ชื่อ/ชั้น/ห้อง freeze ด้วยเหตุผลเดียวกันมาตั้งแต่แรก — เลขที่แค่ตกหล่นไป)
--
-- ข้อมูลเก่าได้ค่าว่าง — เติมย้อนหลังด้วย backfill_0009_class_numbers.mjs "ก่อนขึ้นปีการศึกษาใหม่"

--> statement-breakpoint
ALTER TABLE "entry_members" ADD COLUMN IF NOT EXISTS "class_number_snapshot" varchar(16) DEFAULT '' NOT NULL;
