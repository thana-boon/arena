-- สีของผู้ลงนามบนเกียรติบัตร (ชื่อ + ตำแหน่ง + เส้นสำหรับเซ็นสด)
--
-- ของเดิมฝังสีไว้ใน CertificateCanvas ตายตัว (#1f2937 / #4b5563 / #374151) ปรับจากหน้าออกแบบไม่ได้เลย
-- พอพื้นหลังเป็นภาพโทนเข้ม ชื่อผู้ลงนามก็จมหายไปกับพื้น ทั้งที่บล็อกข้อความอื่นเลือกสีได้มาตั้งแต่แรก
--
-- ข้อมูลเก่าได้ #1f2937 ซึ่งเท่ากับสีชื่อที่เคยฝังไว้ — ใบที่ออกไปแล้วจึงหน้าตาเหมือนเดิม
-- (ตำแหน่งกับเส้นเข้มขึ้นเล็กน้อยจากเทาอ่อนเป็นสีเดียวกับชื่อ)

--> statement-breakpoint
ALTER TABLE "certificate_signatures" ADD COLUMN IF NOT EXISTS "color" varchar(32) DEFAULT '#1f2937' NOT NULL;
