-- ขนาดตัวอักษรของผู้ลงนามบนเกียรติบัตร
--
-- ของเดิมฝังไว้ตายตัวใน CertificateCanvas (ชื่อ 1.2% ตำแหน่ง 1.0% ของความกว้างหน้า)
-- ปรับจากหน้าออกแบบไม่ได้ ทั้งที่บล็อกข้อความอื่นลากมุมย่อ-ขยายได้มาตั้งแต่แรก
--
-- ค่าเริ่มต้น 1.2 = ขนาดชื่อเดิมเป๊ะ ๆ และตำแหน่งยังเล็กกว่าชื่อในอัตราส่วนเดิม (1.0/1.2)
-- แม่แบบที่บันทึกไว้แล้วจึงพิมพ์ออกมาเท่าเดิม

--> statement-breakpoint
ALTER TABLE "certificate_signatures" ADD COLUMN IF NOT EXISTS "font_size" numeric(6, 3) DEFAULT '1.2' NOT NULL;
