-- ไม่มาแข่งขัน (รายคน) — ติ๊กที่หน้าบันทึกผล
--
-- ของเดิมทุกคนที่ลงทะเบียนได้เกียรติบัตรเสมอ ได้ 0 คะแนนก็ยังได้ "เข้าร่วม"
-- คนที่ไม่ได้มาแข่งจึงได้ใบเท่ากับคนที่มา — ติ๊กช่องนี้แล้วจะถูกข้ามตอนออกเกียรติบัตร
--
-- ค่าเริ่มต้น false → ข้อมูลเดิมทั้งหมดถือว่ามาแข่งตามปกติ พฤติกรรมไม่เปลี่ยน

--> statement-breakpoint
ALTER TABLE "entry_members" ADD COLUMN IF NOT EXISTS "absent" boolean DEFAULT false NOT NULL;
