-- เช็คชื่อผู้เข้าร่วม (รายการที่ "ไม่มีการแข่งขัน")
--
-- รายการแบบนี้ไม่มีคะแนนให้บันทึก มีแค่ "มาร่วมกิจกรรมจริงไหม" — ครูติ๊ก "เข้าร่วม" รายคน
-- ที่หน้าเช็คชื่อ คนที่ไม่ติ๊กถือว่าไม่มาร่วม จึงไม่ได้เกียรติบัตร
--
-- ทำไมต้องมีคอลัมน์เวลา: entry_members.absent = false ตอบไม่ได้ว่า "เช็คแล้วว่ามา" หรือ
-- "ยังไม่เคยเช็ค" ถ้าไม่แยกสองอย่างนี้ ครูที่ลืมเช็คชื่อจะออกใบให้ทุกคนที่ลงทะเบียนไว้โดยไม่รู้ตัว
-- → ขาออกเกียรติบัตรของรายการที่ไม่มีการแข่งขันบล็อกไว้จนกว่าจะเช็คชื่อครั้งแรก
--
-- ข้อมูลเดิม: null = ยังไม่เคยเช็คชื่อ → รายการที่ไม่มีการแข่งขันซึ่งยังไม่ได้ออกใบ ต้องเช็คชื่อก่อน
-- (ใบที่ออกไปแล้วไม่ถูกแตะ) ; รายการแข่งขันปกติไม่ใช้คอลัมน์นี้เลย พฤติกรรมไม่เปลี่ยน

--> statement-breakpoint
ALTER TABLE "competitions" ADD COLUMN IF NOT EXISTS "attendance_checked_at" timestamp;
--> statement-breakpoint
ALTER TABLE "competitions" ADD COLUMN IF NOT EXISTS "attendance_checked_by" varchar(64);
